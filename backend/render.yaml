services:
  - type: web
    name: jobconnect-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: jobconnect-db
          property: connectionString